(set-logic ALL)
(declare-const d Date)
(declare-const r1 Date)
(declare-const r2 Date)
(assert (= (date.day d) 5))
(assert (= r1 (date.add (date.add d 0 0 10) 0 0 20)))
(assert (= r2 (date.add d 0 0 30)))
(assert (= r1 r2))
(check-sat)
